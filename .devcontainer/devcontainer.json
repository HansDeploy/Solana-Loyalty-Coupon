{
  "name": "Solana Dev Environment",
  "image": "mcr.microsoft.com/devcontainers/universal:2",
  "features": {
    "ghcr.io/devcontainers/features/rust:1": {},
    "ghcr.io/devcontainers/features/node:1": {
      "version": "18"
    }
  },
  "postCreateCommand": "sh -c \"$(curl -sSfL https://release.solana.com/stable/install)\" && export PATH=\"$HOME/.local/share/solana/install/active_release/bin:$PATH\" && echo 'export PATH=\"$HOME/.local/share/solana/install/active_release/bin:\\$PATH\"' >> ~/.bashrc && cargo install --git https://github.com/coral-xyz/anchor avm --locked && avm install latest && avm use latest",
  "customizations": {
    "vscode": {
      "extensions": [
        "rust-lang.rust-analyzer",
        "anchor-anchor.anchor",
        "solana.solana"
      ]
    }
  }
}
